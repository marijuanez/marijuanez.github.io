<IfModule mod_rewrite.c>
  RewriteEngine On
  
  # Allow POST requests to .php files
  <Files *.php>
    Allow from all
  </Files>
</IfModule>

# Ensure POST is allowed
<Limit GET POST HEAD OPTIONS PUT DELETE>
  Allow from all
</Limit>
